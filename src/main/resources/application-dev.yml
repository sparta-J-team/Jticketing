server:
  port: 8080
  servlet:
    encoding:
      charset: utf-8
      force: true
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ticketing  # 데이터베이스 URL
    username: "root"                       # 데이터베이스 사용자명
    password: "0000"                       # 데이터베이스 비밀번호
    driver-class-name: com.mysql.cj.jdbc.Driver  # MySQL 드라이버 클래스
  sql:
    init:
      mode: always
  h2:
    console:
      enabled: true
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update
#    properties:
#      '[hibernate.default_batch_fetch_size]': 100
#      '[hibernate.format_sql]': true
    show-sql: true
  output:
    ansi:
      enabled: always
  mvc:
    throw-exception-if-no-handler-found: true
  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 10MB
jwt:
  secret:
    key: "ozeoPDLdtIuii8Vt7SxsVZJiK8wQnYRwxZpMZErKX9Y="